

<%= form_with(model: @message, url: messages_path, method: :post, remote: true) do |form| %>
  <%= form.text_area :content, placeholder: 'Type your message...', rows: 2 %>
  <%= form.hidden_field :receiver_id, value: @receiver.id %>
  <%= form.submit  "send"%>
<% end %>